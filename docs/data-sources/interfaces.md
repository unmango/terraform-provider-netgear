---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "netgear_interfaces Data Source - terraform-provider-netgear"
subcategory: ""
description: |-
  Every physical port on the switch, its configuration from show running-config and its live state from show port.
  The port list comes from show port, not the running config, so ports left entirely at their defaults are included. LAG interfaces are reported by netgear_lags instead.
---

# netgear_interfaces (Data Source)

Every physical port on the switch, its configuration from `show running-config` and its live state from `show port`.

The port list comes from `show port`, not the running config, so ports left entirely at their defaults are included. LAG interfaces are reported by `netgear_lags` instead.

## Example Usage

```terraform
data "netgear_interfaces" "all" {}

# The ports with a live link.
output "connected_ports" {
  value = [
    for port in data.netgear_interfaces.all.interfaces : port.port
    if port.link_status == "up"
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `id` (String) Constant `interfaces`. The framework requires an id.
- `interfaces` (Attributes List) The ports, in ascending port order. (see [below for nested schema](#nestedatt--interfaces))

<a id="nestedatt--interfaces"></a>
### Nested Schema for `interfaces`

Read-Only:

- `admin_status` (String) Administrative status reported by `show port`.
- `description` (String) Port description, empty when the switch has none for it.
- `enabled` (Boolean) Whether the port is administratively up. `false` means the config carries `shutdown`.
- `lag` (String) Interface id of the LAG the port belongs to, such as `3/1`. Empty when the port stands alone.
- `link_status` (String) Observed link state, `up` or `down`.
- `mtu` (Number) Maximum frame size in bytes. Zero when the port is left at the switch default.
- `port` (String) Port id in FASTPATH notation, such as `0/1`.
- `pvid` (Number) VLAN untagged ingress traffic is assigned to. Zero when the port is left on the default VLAN.
- `speed` (String) Speed and duplex setting, empty when the port is left at the switch default.
- `tagged_vlans` (Set of Number) The subset of `vlans` the port carries tagged.
- `vlans` (Set of Number) VLANs the port participates in.
